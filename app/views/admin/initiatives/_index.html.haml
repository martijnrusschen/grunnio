%table.table.table-condensed
  %thead
    %tr
      %th= Initiative.human_attribute_name(:name)

  - initiatives.each do |initiative|
    %tr
      %td= link_to initiative.name, [:admin, initiative]
      %td.btn-group
        = link_to admin_initiative_path(initiative), class: 'btn btn-mini' do
          %i.icon-zoom-in
        = link_to edit_admin_initiative_path(initiative), class: 'btn btn-mini' do
          %i.icon-edit
        = link_to [:admin, initiative], :confirm => 'Are you sure?', :method => :delete, class: 'btn btn-mini btn-danger' do
          %i.icon-trash

=link_to t('actions.add', model: Initiative.model_name.human), new_admin_initiative_path, class: 'btn btn-large'
