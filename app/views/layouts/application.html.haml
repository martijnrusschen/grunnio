!!! 5
%html(lang="en")
  %head
    %meta(charset="utf-8")
    =airbrake_javascript_notifier
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title= content_for?(:title) ? yield(:title) : "grunn.io"
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      = javascript_include_tag "http://html5shim.googlecode.com/svn/trunk/html5.js"
    = stylesheet_link_tag "application", :media => "all"
    / %link(href="images/favicon.ico" rel="shortcut icon")

  %body
    = render 'shared/taiga-alerts'
    .container
      .account.whole.text-right
        %ul.steps
          - if account_signed_in? && current_account.is_admin?
            %li
              = link_to(admin_root_path) do
                %i.icon-admin
                admin
            %li
              = link_to(destroy_account_session_path, :method => :delete) do
                %i.icon-signout
                sign out
          -elsif account_signed_in?
            %li
              = link_to(edit_person_path(current_account.person)) do
                %i.icon-user
                ="#{current_account.email}"
            %li
              = link_to(destroy_account_session_path, :method => :delete) do
                %i.icon-signout
                sign out
          - else
            Sign in with:
            %li
              = link_to(account_omniauth_authorize_path(:facebook)) do
                %i.icon-facebook
                Facebook
            %li
              = link_to(account_omniauth_authorize_path(:github)) do
                %i.icon-github
                Github
            %li
              = link_to(account_omniauth_authorize_path(:linkedin))do
                %i.icon-linkedin
                Linkedin
            %li
              = link_to(new_account_session_path) do
                Grunn.io


      %header.whole
        .logo-frontpage
          =link_to(root_path) do
            =image_tag('logo.png')
        .introduction
          %p Grunn.io is een database vol met internet technologie, van de vruchtbare bodem van Groningen.
        %nav
          %ul
            %li= link_to "bedrijven", companies_path
            %li= link_to "personen", people_path
            %li= link_to "initiatieven", initiatives_path

      = yield :main




    / Placed at the end of the document so the pages load faster
    = javascript_include_tag "application"
